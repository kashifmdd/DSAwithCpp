#include<iostream>
using namespace std;

int main() {

// Q1: In a program,input the side of a square.You have to output the area of the square.

    int n, area;
    cout<<"Enter value of side : ";
    cin>>n;

    area = n * n;
    cout<<"Area of square : "<<area<<endl;


// Q2: Enter cost of 3 items from the user(using float data type) - a pencil, a pen and an eraser.You have to output the total cost of the items back to the user as their bill. (Add on: You can also try adding 18% GST tax to the items in the bill as an advaced problem)

    // float pencil, pen, eraser;
    // cout<<"Pencil cost : ";
    // cin>>pencil;
    // cout<<"Pen cost : ";
    // cin>>pen;
    // cout<<"Eraser cost : ";
    // cin>>eraser;
    
    // float totalcost = pencil + pen + eraser;
    
    // cout<<"Total cost of : "<<totalcost<<endl;
    // cout<<"Total cost with GST : "<< (totalcost + (0.18 * totalcost)) <<endl;


// Q3: Build a Simple Interest Calculator. Input : principal(P), rate(R), time(T) Output : (P*R*T)/100

    // float p, r, t;
    // cout<<"Enter Principal : ";
    // cin>>p;
    // cout<<"Enter Rate : ";
    // cin>>r;
    // cout<<"Enter Time : ";
    // cin>>t;

    // float simple_interest = (p*r*t)/100;

    // cout<<"Simple Interest is : "<<simple_interest<<endl;


// Q4: Write a program to calculate the area of a circle.Input : r(radius) Output : PI*r*r(area)

    // float r, area, PI=3.14;
    // cout<<"Enter radius : ";
    // cin>>r;

    // area = PI*r*r;
    
    // cout<<"Area of Circle : "<<area<<endl;

    return 0;
}